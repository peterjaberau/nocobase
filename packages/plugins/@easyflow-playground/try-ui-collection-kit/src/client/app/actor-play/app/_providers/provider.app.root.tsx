import React from 'react';

import { createActorContext } from '@xstate/react';
import { fromPromise, setup, assign, createActor, createMachine } from 'xstate';



export const appRootMachine = createMachine({
  entry: [

  ]
})
