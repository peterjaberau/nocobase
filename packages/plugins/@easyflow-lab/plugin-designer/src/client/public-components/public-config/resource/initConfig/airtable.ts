import { AirtableResource } from "../../../public-types/resource"

export const AirtableResourceInitial: AirtableResource = {
  authenticationType: "personalToken",
  authenticationConfig: {
    token: "",
  },
}
