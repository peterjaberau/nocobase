```bash

#create a new plugin
# 1. stop dev then paste the plugin or use yarn pm create
yarn pm create @easyflow-lab/plugin-json-viewer

# 2. use yarn pm add for the plugin attached to the system
yarn pm add @easyflow-lab/plugin-json-viewer

# 3. go to plugins and enable to


```
